# CognitiveFunctionInvestigation-FineTuning
A look at how fine tuning can effect AI and how different clinical diagnosis's be created only through fine tuning. Other repo's have looks at using the RL training layer and directly modifying connections in LLMs. 
